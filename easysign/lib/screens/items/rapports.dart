import 'package:flutter/material.dart';

class Rapports extends StatelessWidget {
  const Rapports({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Page de rapports")));
  }
}
